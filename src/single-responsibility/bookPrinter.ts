export class BookPrinter {
  printToConsole(text: string): void {
    console.log('book text', text);
  }

  printTextToFile(text: string): void {
    // Code impl to write the text to the file.
  }
}
